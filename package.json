{
  "name": "friend-media-picks",
  "private": true,
  "packageManager": "pnpm@10.18.0",
  "scripts": {
    "dev": "pnpm --filter @friend-media-picks/api dev",
    "typecheck": "pnpm --filter @friend-media-picks/api typecheck",
    "lint": "oxlint --tsconfig apps/api/tsconfig.json apps/api/src",
    "lint:fix": "oxlint --fix --tsconfig apps/api/tsconfig.json apps/api/src",
    "format": "oxfmt --write apps/api/src",
    "format:check": "oxfmt --check apps/api/src",
    "db:check": "pnpm --filter @friend-media-picks/api db:check",
    "db:drop": "pnpm --filter @friend-media-picks/api db:drop",
    "db:migrate": "pnpm --filter @friend-media-picks/api db:migrate",
    "db:reset": "docker compose down --volumes && docker compose up -d",
    "db:studio": "pnpm --filter @friend-media-picks/api db:studio",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "prepare": "effect-language-service patch"
  },
  "devDependencies": {
    "@effect/language-service": "^0.85.1",
    "@types/node": "^25.6.0",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.62.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
