[[language]]
name = "typescript"
auto-format = true
formatter = { command = "pnpm", args = ["exec", "oxfmt", "--stdin-file-path", "%{buffer_name}"] }

[[language]]
name = "tsx"
auto-format = true
formatter = { command = "pnpm", args = ["exec", "oxfmt", "--stdin-file-path", "%{buffer_name}"] }
